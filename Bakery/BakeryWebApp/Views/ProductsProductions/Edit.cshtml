@model BakeryWebApp.ViewModels.ProductProdactionViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Изменение</h1>

<h4>Выпущенные изделия</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProductsProduction.ProductProductionId" />
            <div class="form-group">
                <label asp-for="Date" class="control-label"></label>
                <input asp-for="Date" class="form-control" />
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProductsProduction.ProductId" class="control-label">Наименование изделия</label>
                <select asp-for="ProductsProduction.ProductId" class="form-control" asp-items="ViewBag.ProductId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ProductsProduction.Count" class="control-label"></label>
                <input asp-for="ProductsProduction.Count" class="form-control" />
                <span asp-validation-for="ProductsProduction.Count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-outline-dark" asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
